# Script progressivi UD25 — v6.1

Gli script vanno eseguiti nell'ordine numerico. Ogni file introduce un concetto principale e, dal Task 2 al Task 9, contiene un marcatore esplicito:

```python
# MODIFICA GUIDATA - TASK X
```

| Script | Concetto | Azione guidata |
|---|---|---|
| `01_first_script.py` | esecuzione e `print()` | aggiungere una terza stampa |
| `02_variables_and_types.py` | variabili, tipi e confronto | produrre e correggere un `TypeError` |
| `03_conditions.py` | `if`, `elif`, `else` | provare tre status code |
| `04_lists.py` | lista, `for`, `len`, `max` | aggiungere un elemento con `append()` |
| `05_dictionaries.py` | chiave e valore | aggiungere e leggere `trace_id` |
| `06_list_of_dictionaries.py` | iterazione su richieste | inserire un filtro `if` |
| `07_functions.py` | parametri e `return` | richiamare la funzione con una seconda soglia |
| `08_read_csv_first_rows.py` | `DictReader` e conversioni | aggiungere `trace_id` all'output |
| `09_count_errors.py` | integrazione | leggere e spiegare il flusso completo |

Il laboratorio autonomo parte da `starter/summarize_requests_basic_TODO.py`.

Nessuno script richiede pacchetti esterni: `csv` e `pathlib` fanno parte della libreria standard Python.

# UD28 — Mini-attività

## 1. Tre file

Associare:

```text
baseline_products_requests.csv
evaluation_products_requests.csv
products_reference_labels.csv
```

alle funzioni:

```text
costruire / testare / valutare
```

## 2. Stessa osservazione?

Spiegare perché `eval-005` compare sia nel file evaluation sia nel reference file.

## 3. Chi crea la reference?

Indicare almeno tre possibili fonti o stakeholder che possono contribuire alla validazione.

## 4. Reason o root cause?

Classificare:

```text
"Status HTTP 500 inatteso"
```

come:

- motivo della label;
- oppure root cause.

Spiegare la scelta.

## 5. Falso negativo

Soglia `196,5`, durata `190`, status `500`, reference `anomaly`.

Spiegare perché un detector che usa solo la durata produce un FN.

# Query KQL UD09

Ogni file KQL contiene commenti interni che spiegano scopo, passaggi e limiti della query.

Le query da `01` a `07` usano un dataset simulato con `datatable` e sono quindi indipendenti dalle tabelle reali del workspace.

Le query opzionali su tabelle reali vanno eseguite solo quando le tabelle sono presenti nel Log Analytics Workspace.

# Esempi di provisioning alerting

Questi file sono intenzionalmente fuori da `grafana/provisioning/alerting`, quindi Grafana non li applica automaticamente.

Servono per mostrare, dopo il laboratorio GUI-first, che contact point e regole possono essere gestiti anche come configurazione versionata.

# docs

Cartella per report e documentazione prodotti dal partecipante.

Template disponibili:

```text
template_report_ud07.md
template_report_ud07_autonomo.md
```

I report compilati non devono restare generici. Ogni conclusione deve puntare a un file in `evidence/`, a un log in `logs/` o a uno screenshot in `img/`.

# Query KQL locali

Queste query usano `datatable()` e quindi non dipendono dalla presenza di log reali nel workspace.

Sono utili per esercitare operatori come `where`, `project`, `summarize`, `sort by` e `take` con dati prevedibili.
